name: ci

on:
  push:
  pull_request:

jobs:
  macos-test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '25.x'
      - run: npm install
      - run: npm test
